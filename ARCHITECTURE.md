# 🗺️ 英語学習アプリ — 設計図（アーキテクチャ全体像）

> このドキュメントは「**プロンプトを使いこなす学習者育成アルゴリズム**」を別スレッドで構築するための**土台（基盤資料）**です。
> 既存アプリの全体構造・データモデル・育成ロジック・拡張ポイントを1ファイルにまとめています。
> 末尾の「§8 拡張ポイント」が新アルゴリズムの接続先です。

- 公開URL: **https://kenjiintasmania.github.io/kenjiyamada/**
- リポジトリ: `kenjiintasmania/kenjiyamada`（正本＝`master`、GitHub Pages 直接公開）
- 構成: **静的HTML/JS（ビルド不要）＋ Google Apps Script（GAS）バックエンド＋ Googleスプレッドシート**

---

## 目次
1. [これは何か（概要と設計思想）](#1-これは何か)
2. [全体設計図](#2-全体設計図)
3. [コンポーネント](#3-コンポーネント)
4. [データモデル（最重要）](#4-データモデル最重要)
5. [主要フロー](#5-主要フロー)
6. [育成エンジン（既存の学習者育成ロジック）](#6-育成エンジン)
7. [品質ゲート・問題工場](#7-品質ゲート問題工場)
8. [拡張ポイント（新アルゴリズムの接続先）](#8-拡張ポイント)
9. [デプロイ・運用](#9-デプロイ運用)
10. [守るべき制約・原則](#10-守るべき制約原則)

---

## 1. これは何か

中学生向けの英語学習アプリ集（山田先生）。**3つの学習アプリ＋マイページ＋先生用管理ページ**で構成。

| 観点 | 設計判断 |
|---|---|
| 動作環境 | ブラウザだけで完結。インストール不要、スマホ可。 |
| 保存 | 生徒の記録は **localStorage**（端末ローカル）。サーバー必須にしない。 |
| 集約 | 先生に届ける時だけ **GAS 経由でスプレッドシート**に集約（1人1行 upsert）。 |
| バックエンド | **GAS ウェブアプリ1本**（`tools/score_gas.gs`）。CORSプリフライト回避のため `text/plain` POST。 |
| 人の関与 | 単元テストの開始/終了・学習方針は**先生がゲート制御**（teacher-in-the-loop）。 |
| 品質保証 | 全コンテンツが `npm run check`（実エンジン採点＋構造検査）を通る。CI＋自走スレ（問題工場）。 |

**設計の背骨**：「生徒＝ローカルで練習・記録」「先生＝集約された成績を見る・方針を出す」「コンテンツ＝機械検証で品質担保」。この3者を疎結合につなぐのが GAS と localStorage。

---

## 2. 全体設計図

```
                         ┌──────────────────────────────────────────────┐
                         │            ブラウザ（生徒の端末）              │
                         │                                              │
  index.html (トップ) ──▶│  📕 words/   📝 mogi/   🎯 eiken/   📊 me/    │
                         │     │           │          │         │       │
                         │     └─────┬─────┴────┬─────┘         │       │
                         │           ▼          ▼               ▼       │
                         │     ┌───────────────────────────────────┐   │
                         │     │   localStorage（同一オリジン共有）   │   │ ← 生徒の「センサーデータ」
                         │     │  tango_v2 / mogi_best_v1 / shuku_v1 │   │
                         │     │  mado_*（本人情報・方針・スナップ）    │   │
                         │     └───────────────────────────────────┘   │
                         └───────────────────────┬──────────────────────┘
                                                 │ fetch POST (text/plain, JSON)
                       先生 ─▶ admin/index.html  │  ・kind:"summary"（マイページ集約）
                          （PIN でゲート/方針）    │  ・kind:"unittest"（単元テスト提出）
                                                 │  ・action:"gate/status/policy/setpolicy"
                                                 │  ・(kind無し)＝英検1回ごと
                                                 ▼
                         ┌──────────────────────────────────────────────┐
                         │      Google Apps Script  tools/score_gas.gs   │  ← 単一デプロイ（URL固定）
                         │   doPost ルーター → 各ハンドラ → スプレッドシート │
                         └───────────────────────┬──────────────────────┘
                                                 ▼
                         ┌──────────────────────────────────────────────┐
                         │            Google スプレッドシート（先生）       │
                         │  成績まとめ / 英検テスト履歴 / 単元テスト記録 /   │
                         │  単元管理 / 設定    ＋「📊 成績ツール」メニュー    │
                         └──────────────────────────────────────────────┘

       ── 品質の輪（問題工場）─────────────────────────────────────────
        master ⇄ qa/loop・gen/predict・gen/okayama（ブランチ＋PR）
        tools/check_exams.mjs（npm run check）＝ CI 品質ゲート（.github/workflows/exam-check.yml）
```

---

## 3. コンポーネント

| パス | 役割 | 主要技術・キー |
|---|---|---|
| `index.html` | 統合トップ。3アプリ＋マイページへの入口。 | 絵文字favicon、カード型ナビ |
| `words/` | **単語アプリ**（基本編435＋拡張編1565＝2000語＋**活用編**200パターン/600点）。訳→英語タイピング式。 | `js/app.js`、`data/words.js`、`data/katsuyo.js`、`localStorage: tango_v2` |
| `mogi/` | **模擬テスト**（中2×3・中3×4）＋**単元テスト**（c2u1/c2u2/c3u1/c3u2）＋**習熟度単語テスト**。 | `assets/engine.js`、`data/*.js`、`exam.html`、`vocab_*.html` |
| `eiken/` | **英検アプリ**（7〜2級・級判定・4技能・長文）。`mado_loop.html` 収録。 | 単一HTML（2000行規模）、`localStorage: mado_*` |
| `challenge/` | **挑戦モード**（練習専用・記録なし）。①単語ランダム50問（基本/拡張）②学年別**並べ替え英作文**（単語タップで組立・自動採点／自己採点は廃止）③英検二次プロンプト生成。 | `index.html`、`data/compose.js`（`{g,jp,answer,words,tip}`）、words.js再利用 |
| `aimode/` | **AI練習メニュー・探究カルテ・発表カルテ**（`index.html`／`mypage.html`／`pres.html`）。**プロンプト設計スレッドが所有**するアプリ群で、当スレッドは配置と接続だけを担当する（ロジックは無改変）。`index.html` の診断は**当アプリの記録をそのまま読む**（`tango_v2`／`mogi_best_v1`／`shuku_v1`／`mado_bestfull_v1`／`mado_levelavg_v1`／`mado_loop_lasttest_v1`）。学年・番号は `mado_year`／`mado_num` を共有。**同じオリジンに置くことが接続の条件**なので、リポジトリ直下の `aimode/` に置く（3画面の相対リンクも `../index.html`／`../me/index.html` 前提）。 | 送信先は `log_gas.gs`＝**別プロジェクト・別URL**（`AKfycbw6…`）。書込先は同じブックの「AImodeログ」タブで、`score_gas.gs` の `buildCorrelationTab()` が**学年＋番号**で成績と突合する。当スレッドの追記は3か所だけ：①`assets/site.js` の読み込み ②`LOG_GAS_URL` の直後に `SITE.gasFor("aimode", …)` ③送信直前に `SITE.tag(payload)`。**これが無いと実証モードでAIモードの記録だけ生徒用ブックへ落ちる**（`aimode` 未設定なら送信が止まる）。 |
| `dojo/` | **読解道場**（入試読解の3層訓練：①テクカードで学ぶ→②スキル種目で鍛える→③岡山県模試10本で実践）。テクは先生由来（英検アプリ「中学入学準備」P01〜P10・語順メソッドW01・リスニングメモ術T11・**図形分析法G01〜G03**）＋提案T01〜T10。種目は S1根拠さがし（大問5対策）・**S2図形分析**（先生の図形分析法）・S3図表突合（大問2対策）、各12問・1回8問ランダム・全自動採点。S2 は英文1文を見せ、記号（be動詞＝／知覚動詞≒／一般動詞V／前置詞の前に／／to→／from←／and＆／but←→／than＞／肩書のカンマ＝／前置詞のない副詞・時場所は（　））のつけ方を選ばせる。答え合わせでは**記号をつけた形の文**を見せる。ねらいは日本語に訳さずに骨組みをつかむこと（単語が分からなくても "ate ＆ walked" で動詞2つと読める）。※ `my_words.xlsx` の暗唱教材では（　）を冠詞・所有格に使っており用途が違う。混同しないよう別物として扱う。誤答時に根拠(why)/ワナ(trap)/関連テクを表示し、結果画面から弱いテクカードへ誘導（閉ループ）。 | `index.html`、`data/tech.js`（`{id,cat,name,from,what,how[],drill[]}`）、`data/drill_s1.js`/`drill_s2.js`/`drill_s3.js`（`{id,tech[],q,choices,answer,why,trap}`）、`localStorage: dojo_v1`（GAS非連動・第2弾でスキルタグ模試連携とスプシ列を検討） |
| `me/` | **マイページ**。3アプリの記録を集約表示＋先生送信＋**育成コメント**。 | `index.html`、`localStorage` 読取、`SUMMARY_GAS_URL` |
| `admin/` | **先生用管理ページ**。単元テストのゲート開閉・学習方針設定。 | PIN認証、5秒ポーリング、stale検出 |
| `jigaku/` | **自学マイページ**（通常授業の自学促進・第1弾＝単語レーン）。①**生徒が自分の単語リストを用意する**（打ち込み／自分でまとめたリストのスクショ）→②プロンプトを自分で組み立てる→③外部AIに出題させて自分で答える→④提出ブロックを貼る→⑤**アプリ側が再採点**→記録・送信。**AIの丸つけは一切使わず**、貼られた「わたしの答え」と「正解」の一致だけで判定する（探究カルテと同じ思想）。**出題の根拠は生徒が作ったリストだけ**で、教科書名・単元名からAIに推測させない（教科書は学年・地域で変わるため、単元名は根拠にならない）。打ち込みの場合はAIが出した「正解」をそのリストと突合し、リスト外の語を混ぜていないか検算する。「調べる→意味を書く→まとめる」までが自学で、その成果物を持ってくる場所という位置づけ。固定するのは提出ブロックの書式だけで、中身は生徒が自由にチューニングでき、版管理（v1→v2…）で工夫を引き継げる。書式を守らせること自体をプロンプト運用スキルの訓練として扱う。 | `index.html`、`localStorage: jigaku_v1`（list／prompts[]／runs[]）。貼り付けの解析は3段構え（通常→改行復元→縦棒だけで4列を組み直す）で、markdownの表・1行貼り・番号が前の語にくっついた形をすべて拾う。先生送信は既存スプシの新タブ「自学ログ」へ1回ごとに追記（`kind:"jigaku"`）。旧版GASは未知の kind を英検タブに書いてしまうため、送信前に `action:"policy"` で版を確かめ、`jigaku` を含む版のときだけ本送信する。文法レーンは `bunpo.html`＋`bunpo.js`（下記）。本文レーンは準備中 |
| `jigaku/bunpo.html` | **文法レーン**（第1弾＝現在完了形）。①**覚える一覧を読む**（基本文9文・訳・解説）→②基本文を**見ながら**打つ→③日本語訳だけを見て打つ→④**AIモードで10問**（選択5＋並べかえ5）→⑤まとめを貼って再採点。**基本文はAIに作らせない**。学年・教科書によらず同じものを教えたいので `jigaku/data/bunpo_kanryo.js` に固定で持ち、①〜③はアプリの中だけで完結する。AIに任せるのは④の**問題のランダマイズだけ**で、しかも**1問ずつ出させる**（10問まとめて出すと並べかえは書く量が多すぎて手が止まるため、10往復＋まとめ1回という形にしている）。英作文は入れない（正解が一意でなく機械採点できないため）。③は基本文を先に見せているので作文ではなく**再生**＝正解は一つ。配点は 再生4点／文（つづりミス −1）・選択2点（**一致のみ**。選択肢はどれも別の語なので1文字ちがいをつづりミス扱いしない）・並べかえ4点（**完答のみ**）。②の写しは点にせず「写しまちがい」の数だけ出す。語彙は**中1に固定**（完了形を習う学年が教科書ごとに違うため、先取りの生徒が単語で止まらないように）。基本文は `words.js` の中1語と `katsuyo.js` の過去分詞だけで書いてあり、AIの出題も同じ基準で照合して外れた語は**警告のみ**で止めない。`already / yet / ever / since` などは中3語だが完了形に不可欠なので例外にする。 | `bunpo.js`＋`data/bunpo_kanryo.js`、`localStorage: jigaku_v1.bunpo`（prompts[]／runs[]）。送信は `kind:"jigaku"` の `lane:"文法"`、リスト名の位置に項目名、`notDb` に写しまちがいの数。 |
| `jigaku/honbun.html` | **本文レーン**。①読んだ本文を持ってくる（**打ち込む**／自分でまとめたノートのスクショ）→②AIモードで**3種5問**（数字・人名・地名／接続詞／重要だと思う情報）を**1問ずつ**→③まとめを貼って再採点。打ち込みは `contenteditable` で、**Ctrl+B とボタンで太字**にして強調させる。太字はアプリが機械的に取り出せるので、**「強調した語のうち、あとの設問で答えに絡んだのは何語か」を突合**でき、メモの精度をAIの判断に頼らず数値にできる。AIには「メモの精度に一言」だけ言わせ、**点数には一切使わない**（音の道場のスピーキングと同じ型）。強調が無い場合はコメント要求を出さず、いきなり出題に入る。配点は1問4点＝20点満点。単一解答は一致で満点・1文字ちがいは −1（本文からの抜き書きなので、読み取れたかが主眼でつづりそのものは問うていない）。**列挙は集合で比べ、合った数で按分**し、よけいに挙げた分は減点しない。打ち込み経路では**AIが正解として返した語が本文に実在するか検算**する（複数語の答えは語に割って、どれ1つも本文に無いときだけ警告＝空振りを避ける）。スクショ経路は本文を持たないので検算できず、その旨を画面に出す。 | `honbun.js`、`localStorage: jigaku_v1.honbun`（src／prompts[]／runs[]）。送信は `kind:"jigaku"` の `lane:"本文"`、`listN` に本文の語数、`notDb` に本文外の語、`mark`/`hit` に強調数と的中数。 |
| `listening/` | **音の道場**（疑似リスニング／疑似スピーキング）。**音声ファイルを1つも持たずに**リスニングを載せるための仕組みで、ブラウザ内蔵の `speechSynthesis` で英文を読み上げ、`SpeechRecognition` で答える。①素材を作らせるプロンプト→②AIモード→③貼る→④聞いて答える（スクリプトは採点まで隠す・再生回数は素材の指定どおりに制限・ゆっくり切替）→⑤自動採点→⑥スクリプト開示。スピーキングは機械採点できないので**点をつけず**、生徒自身の発話から測れる観測値（語数・1問あたり・質問の語の再利用・つなぎ語の種類）だけを出し、AI講評用の文面は「点数はつけないでください」と書いた形で生成する。読み上げ／マイクが使えない端末では、スクリプト表示と打ち込みで同じ練習が成立する（`onend` が来ない端末向けに再生表示の保険タイマーあり）。 | `index.html`、`localStorage: listening_v1`（lRuns[]／sRuns[]）。素材の書式は `=== LISTENING === / --- SCRIPT --- / --- QUESTIONS ---`、質問は `番号 \| 質問 \| 選択肢… \| 正解番号`。二次は `=== SPEAKING ===` |
| `trial/` | **実証用セット（試用版）**。外部での実地検証（麻生情報システム）の入口。アプリ本体は生徒用と同一のまま、送信先だけを実証用スプレッドシートに切り替える。状態表示・接続テスト（`action:"ping"`・書き込みなし）・解除ボタン。 | `index.html`、`SETUP.md`（手順書）、`assets/site.js`、`tools/score_gas_trial.gs` |
| `assets/` | **サイト（送信先）切替の共通モジュール** `site.js`。`?site=aso` または端末記憶で実証モードを判定し、`SITE.gasFor(kind, 既定URL)` で送信先を解決する。通常モードでは既定URLをそのまま返す＝**生徒の経路は無改変**。実証モードで未設定なら `""` を返し送信を止める（生徒用ブックへ落ちない安全側の失敗）。実証中は全画面先頭に帯を出し、60日で自動失効。 | 全13画面が読み込み。送信点は `me`/`mogi/exam`/`eiken`/`admin` の4箇所のみ |
| `tools/score_gas.gs` | **バックエンド**。成績集約・単元ゲート・方針・英検・成績ツールメニュー。 | GAS、LockService、スプレッドシート |
| `tools/check_exams.mjs` | **品質ゲート**。全模試を実エンジンで採点→100点＋構造＋単語サニティ。 | Node＋jsdom、`npm run check` |
| `factory/` | **問題工場**のハブ・ディスパッチ・QAレポート。 | `README.md`、`dispatch.md`、`reports/` |
| `.github/workflows/exam-check.yml` | CI。push/PR/毎週月曜/手動で品質ゲート実行。 | GitHub Actions |

---

## 4. データモデル（最重要）

新アルゴリズムが読む「センサー入力」と書く「介入先」は、ほぼすべてここにある。

### 4a. localStorage（生徒のローカル記録＝同一オリジンで全アプリ共有）

| キー | 形 | 意味 | 書く人 |
|---|---|---|---|
| `tango_v2` | `{cleared:{id:ts}, weak:{id:ts}, settings:{retry,sound,order}}` | 単語アプリ：打ち込めた語ID集合＋**苦手フラグ**（基本編/拡張編のみ対象。ミスで立ち正解で下りる＝背景記録）＋設定。**活用編(katsuyo)も cleared キーは共有**（id空間は文字列"v1_0"等で数値idと非衝突。weakは活用編には書かない） | words |
| `mogi_best_v1` | `{examId:{title,best,last,total,ts}}` | 模試：各回の最高/直近点（100点満点） | engine |
| `shuku_v1` | `{c2:{best,max,count,last}, c3s2:{...}}` | 習熟度単語テスト：最高点・受験回数 | vocab_*.html |
| `mado_year` / `mado_num` / `mado_name` | string | 本人情報（学年1〜3／番号＝半角／名前） | me, exam, eiken |
| `mado_policy` | string | 先生の学習方針コード（送信時にサーバーから受信しキャッシュ） | me |
| `mado_snap_v1` | `{mBest,vBest,act,day}` | 前回スナップショット（ほめ言葉の差分計算用） | me |
| `mado_bestfull_v1` / `mado_bestquick_v1` | number | 英検：本番/クイックの最高レベル座標(0〜8) | eiken |
| `mado_levelavg_v1` | `{sum,count}` | 英検：平均レベルの目安 | eiken |
| `mado_loop_lasttest_v1` | `{grade,pct,pass}` | 英検：直近の判定テスト結果 | eiken |

> **重要**：localStorage はオリジン単位。3アプリ＋マイページは**同じ記録を共有**する。
> 「打ち込めた語数」「模試ベスト」「習熟度%」「英検レベル」が、生徒1人の状態ベクトル。

### 4b. GAS スプレッドシート（先生の集約ビュー）

| シート | 役割 | 主キー | 更新方式 |
|---|---|---|---|
| `成績まとめ` | 3アプリ最新/最高を**1人1行**。固定列（`単語_活用`まで）の右に `自学_U3` `自学_L5` … が自動生成される | 学年/番号 | upsert（`max`列は下げない・他は最新上書き）＋自学列は毎回作り直し |
| `英検テスト履歴` | 英検の**1回ごと**追記 | — | append |
| `単元管理` | 各単元テストのゲート状態 | 試験ID | 状態(開/閉)・セッション・提出数 |
| `単元テスト記録` | 単元テスト提出を1回ずつ | セッション+試験+学年+番号 | append（同一で重複拒否） |
| `自学ログ` | 自学マイページの1回ごと（レーン/リスト名/作り方/リスト語数/問題数/正解数/正答率/プロンプト版/リスト外の語/まちがえた語/クリアした語/強調数/的中数） | — | append |
| `設定` | 学習方針コード（B2） | — | 先生がadminから設定 |

`成績まとめ` の列定義は `score_gas.gs` の `SUMMARY_COLS`（`{key, head, max}`）が単一の真実。
マイページの `buildPayload()` のキーと1対1で対応する（＝**ここに列を足せば集約に乗る**）。

### ユニット別クリア語数（`自学_◯` 列）

`rebuildJigakuUnits()` が `自学ログ` を読み、**固定列の右**（＝`SUMMARY_COLS` の幅の外）に
ユニットごとの列を作る。固定列側は幅 `SUMMARY_COLS.length` までしか読み書きしないので、
既存の集約とはぶつからない。

- ユニットは**リスト名から取り出す**（`Unit 3` / `unit3` / `U3-1` → `U3`、`Lesson 5` / `レッスン5` → `L5`、
  取れなければ `その他`）。教科書名や単元データは持たない＝**教科書に依存しない**。
- `自学ログ` の集計は**レーンで分ける**。`レーン` が「単語」以外（文法・本文…）の行は語数ではなく
  **その項目の最高正答率**で `文法_現在完了形` `本文_Unit 3-1` のような列に出す。
  単語の語数集計に混ぜると項目名が `その他` に落ちて数字が汚れるため。
- アプリ側は `Unit`/`Lesson`・**ユニット番号（0〜15）**・**何つめのリストか（1〜15）**の3つを
  プルダウンで**必須選択**にし、揃わないと次の画面へ進ませない。リスト名は `Unit 1-2` の形に固定され、
  自由記入は持たない。自由記入だと「1」「小テスト」のような名前になり `その他` に落ちて
  集計にならないため、ここは注意喚起ではなく**システムで縛る**。送信直前にも形式を再確認する。
- 教科書によってユニット内の区切り方が違うので、**細分は生徒が自分で決める**（1つめ・2つめ…）。
  ただし**列は1ユニット1列**にまとめる。`U1-1` と `U1-2` を別列にすると、両方に入っている語を
  二重に数えてしまい「同じ語は1語」が崩れるため。何つめのリストかは `自学ログ` のリスト名に残る。
- 過去ぶんの持ち越しは、`自学ログ` に **「クリアした語」を空にした行**を足す（リスト名は `これまでの分` など）。
  正解数がそのユニットの下限値として効き、実数が超えたら自然に切り替わる。
  **`成績まとめ` に手で打った数字は次の送信で消える**（自学列は毎回作り直すため）ので、必ずログ側に足すこと。
- 値は「そのユニットで**これまでに正解できた語の実数**」。同じリストを何回やっても同じ語は1語。
  1つのユニットを複数のリストに分けても合算される。`クリアした語` が無い古い記録は
  そのユニットの正解数の最大値で代用する。
- `自学ログ` に居て `成績まとめ` に行が無い生徒は、行を作ってから書く（自学だけの生徒を落とさない）。
- 実行契機は ①`kind:"jigaku"` の受信時（自動）②メニュー「🔤 自学ログ▶ユニット別クリア語数を更新」。
  同時実行は `LockService` で1つに絞り、取れなければ次の送信でやり直す。

### 4c. コンテンツのスキーマ

**単語**（`words/data/words.js`、`build_words.py` が自動生成）:
```js
window.WORD_META = {total, target, posOrder, basicCount, extendedCount,
                    basicByPos, extendedByPos, droppedCount};
window.WORDS = [{ id, w /*英語*/, j /*訳*/, p /*品詞*/, m:"b"|"e" /*基本/拡張*/,
                  g:1|2|3 /*学年*/, alt?:[/*別表記も正解*/] }, ...];
```

**活用編**（`words/data/katsuyo.js`、`words/tools/build_katsuyo.py` が `my_words.xlsx`
の「7動詞の変化形」「過」「8比較表現」タブから自動生成）: 動詞150＋形容詞50＝200パターンを
1パターン＝3疑似単語（原形→過去形→過去分詞形／原形→比較級→最上級）に展開し、`WORDS` と
同じ `tango_v2.cleared` を共有（＝1語形=1点・打ち込めた語数の仕組みをそのまま再利用）。
`words/js/app.js` はモード `'k'` の出題順を **kid（パターンID）単位でシャッフル**し、
slot 0→1→2 の3連続を必ず守る（個別語をシャッフルしない・再挑戦注入もしない）。
```js
window.KATSUYO_META = {verbCount:150, adjCount:50, patternCount:200, maxScore:600};
window.KATSUYO_WORDS = [{ id /*"v1_0"等*/, w, j /*意味＋開示済み語形*/, p:"動詞の活用"|"形容詞の比較",
                          m:"k", kid /*パターンID*/, slot:0|1|2, slotLabel, alt? }, ...];
```

**模試/単元テスト**（`mogi/data/*.js`、`engine.js` が描画＆採点）:
```js
window.EXAM = {
  id, title, unit?:true /*単元テスト＝mogi_best保存せずGAS提出*/,
  sections:[{
    no, title, lead?,
    courses?:[{name,intro?,items}],          // 大問6 X/Yコース選択
    groups?:[{ intro?, script?/*放送文*/, sceneNote?, flyer?,
               passage?, passageEn?, note?, items:[ITEM] }]
  }]
};
// ITEM 型（自動採点）:
//  mcq      {stem,choices,answer:idx,pt,label}
//  mcqMulti {answer:[idx,...]}
//  fill     {answers:[...],hint}      ← 表記ゆれ正規化で照合
//  bankpick {bank,answer:idx}         ← 共通選択肢
//  wordorder{words:[...],answer:"完成文"}  ← タップ並べかえ
//  en / jp  {model,minWords?}         ← 模範解答提示・自己採点
```

---

## 5. 主要フロー

**(1) 採点（模試/単元）** — `engine.js`
```
MockExam.render(EXAM,#quiz) → 生徒が解答 → MockExam.gradeAll()
  → 各ITEMを採点・○✕表示 → 合計/満点を .scorebar に表示
  → window.MockExam.last={score,total,pct,id}
  → EXAM.id かつ !EXAM.unit のとき mogi_best_v1 に最高点保存
  → 一番上へスクロール
```

**(2) 単元テストのゲート** — `admin` ⇄ `exam.html` ⇄ GAS
```
先生: admin で PIN＋[スタート] → action:"gate"{open:true}
   → GAS が 単元管理 にセッション "S+日時" を発行（LockService）
生徒: exam.html?id=c2u1 が 5秒ごとに action:"status" でポーリング
   → 受付中になったら採点UI解放 → 提出 kind:"unittest"
   → GAS: ゲート開＋同一(セッション+試験+学年+番号)未提出のときだけ記録（1人1回）
```

**(3) 成績集約・送信** — `me` → GAS
```
マイページを開く → localStorage 全読み取り → buildPayload()
  → kind:"summary" を POST → GAS が 成績まとめ に upsert
  → 戻り値で現在の学習方針 policy を受信 → コメント更新
  ※ 習熟度単語は 80%以上のときだけ正式点を送る（回数は常に送る）＝水増し防止
```

**(4) 学習方針 → コメント** — `admin` → GAS → `me`
```
先生: admin で方針を選び保存（action:"setpolicy"｜PIN）→ 設定シート B2
生徒: マイページ送信の戻り or action:"policy" で方針を取得
   → guidance(policy, records) が「次の一手」を1行表示
```

---

## 6. 育成エンジン

> **ここが既存の「学習者育成ロジック」。新アルゴリズムはこの考え方を継承・自動化する。**

マイページ（`me/index.html`）の `📣 きょうのミッション` カードは、2行で生徒を動かす：

- **ほめ1行** `praise(R, snap)`：前回スナップショット `mado_snap_v1` との**差分**で文面を選ぶ
  （模試が上がった→「○点アップ！」、活動が増えた→「挑戦してますね！」、下がっても→「続けててえらい」…）。
- **指針1行** `guidance(policy, R)`：先生の**方針コード**に応じて「次にやること」を出す。

方針コード（`POLICIES` / admin の選択肢）:
```
kihon（基本単語） / shuku（習熟度単語） / kihon_shuku /
moshi（模試） / eiken（英検） / balance（バランス）
```
`guidance()` は方針ごとに、生徒の状態 `R`（模試ベスト・習熟度%・基本語進捗・活動量…）を見て
「未達なら優先タスク／達成なら次ステージ」を分岐する。

**＝これは小さな制御ループ**：
```
状態を測る(records) → 方針(teacher) → 介入(praise+guidance) → 次回また測る
```
現状は方針が**手動**・分岐が**ルールベース**。新アルゴリズムはこのループを
**データ駆動・適応的**にする余地が大きい（→ §8）。

`records()` が返す状態ベクトル（育成アルゴリズムの入力に直結）:
```
mBest, mDone,                  // 模試 最高点・挑戦回数
vC2, vC3, vBest, vC2n, vC3n,   // 習熟度単語 %・回数
wBasic, wBasicT, wAll,         // 基本語 進捗・総語数
act,                           // 活動量（模試回数+単語テスト回数+単語+英検）
day                            // 今日の日付（連続性判定）
```

---

## 7. 品質ゲート・問題工場

**品質ゲート** `tools/check_exams.mjs`（`npm run check`）:
1. 全11模試を**実エンジンで採点 → 全問正解で 100/100** になるか（出題と採点の整合）
2. 構造検査（mcq/bankpick の answer 索引、wordorder の語が answer 内にあるか 等）
3. 単語サニティ（相対参照の訳・空訳・**答えバレ**＝英単語が訳に露出）
- CI（`exam-check.yml`）が push/PR/毎週/手動で実行。**PRはここが緑でないとマージしない**。

**問題工場（ツリー）** `factory/`:
```
master（正本）
├─ qa/loop      ① 品質チェックを巡回 → AIレビュー＋反証検証 → 修正/レポート → PR
├─ gen/predict  ② 年間予定から予測模試を先行生成 → PR（要：annual_plan 入力）
└─ gen/okayama  ③ 岡山入試 reference-only（傾向だけ参照しオリジナル新作）→ PR
```
自走は2層：**A=GitHub Actions（持続・無料の検査役）／B=Claude Code on web のスケジュール（作問・レビュー）**。
PRのマージ＝**人（先生）のレビュー関門**。

---

## 8. 拡張ポイント

> **「プロンプトを使いこなす学習者育成アルゴリズム」を別スレで作るときの接続マップ。**
> 既存の疎結合構造をそのまま使えば、最小の改変で「新しい学習軸＋その育成ループ」を足せる。

> ⚠️ 混同注意：基本アプリ側の **挑戦モード（`challenge/`）の「英検二次プロンプト生成」は別物**。
> あれは固定テンプレをコピペするだけの **“面接の練習用”** で、データ収集も評価もしない（基本レーン内）。
> §8 が扱うのは **プロンプト運用スキルそのものを育てる AIモード**（収集・分析・蒸留つき／§8.0 の2レーン分離）で、目的も構造も異なる。

このアプリは既に **「学習サーフェス → ローカル記録 → 集約 → 育成コメント」** の型を持っている。
新アルゴリズム＝**プロンプト運用スキルという第4の学習軸**を、同じ型に乗せるのが素直。

### 8.0 必須要件：いつでも切り離せる「2レーン分離」🔌

> プロンプトモードは基本アプリと**コードは疎結合**で並走させる（専用GAS・ON/OFF）。
> 一方、**学習データは成績と突合できるよう同一ブックの専用タブに意図的に統合**する（下の(2)・確定方針）。
> ＝「コード/UIは分離、データは相関のため同居」。**この章の他の提案より優先。**

**(1) 接続 ON/OFF（管理者ボタン）**
- 先生用 `admin/` に「**プロンプト連携 ON/OFF**」トグルを置く（PIN必須・`gate`/`policy` と同じ流儀）。
- 状態はフラグとして保存（例：プロンプト専用GASの `設定` シート、または `prompt_enabled`）。
- **ON のとき**：トップ `index.html` にプロンプトのカードを出し、マイページにプロンプト育成欄を表示。
- **OFF のとき**：基本アプリ（`words`/`mogi`/`eiken`/`me`）は**今日と完全に同じ挙動**。プロンプト関連UIは出さず・通信もしない。
- **フェイルセーフ**：フラグ取得に失敗したら **OFF 扱い**（プロンプト側の障害が基本アプリを止めない）。

**(2) プロンプト学習データは「成績と同一ブックの専用タブ」（意図的に統合 ✅確定）**
> 旧方針（別ブックに隔離）から**変更**。成績×プロンプト力を**生徒単位で突合**するため、
> 成績管理スプシ（grade book）の中に**専用タブ「AImodeログ」**として集約する。
- **コードは分離**：プロンプト側は**専用GAS `log_gas.gs`（score_gas とは別デプロイURL＝`LOG_GAS_URL`）**。
  `score_gas.gs`／`SUMMARY_COLS`／`成績まとめ` タブは**触らない**（grade lane は不変）。
- **データは同居**：`log_gas.gs` は同一ブックの「AImodeログ」タブに **append** するのみ。
  識別子（**学年＋番号**）が `成績まとめ` と共通なので、そのキーで join して相関分析できる。
- **切り離し**：app/URL 単位では今も外せる（AImode ページ＋`LOG_GAS_URL` 定数を消す）。
  データは成績ブック内に残る＝**完全分離より「相関のしやすさ」を優先**した判断。

#### 実装の現況（検証済み・2026-06-26）
- フロント：`mado-drill/AImode.html`（基本アプリと**同一オリジン**＝`mado_*` 等の localStorage を共有）。
  記録から **レベル座標 L(0〜8)＋段階(知識/思考)＋弱点** を診断し、5モード（①知識/②思考/③議論/④探求/⑤フリーパス）＋英検二次のプロンプトを**ゲート付き**生成。ゲート：①常時／②単語80% or 英検3級 or 先生／③④⑤ 先生モード。
- 送信：隠しフォーム→隠しiframe POST（CORS非依存）。payload＝`{kind:"ai_log", ver, cls, num, name, level, raw}`。
- サーバ：`log_gas.gs` が `e.parameter.payload`（フォーム）／`postData.contents`（生POST）両対応で受け、`raw`（「まとめ」8項目）を**ラベル基準**で列に分解＋**AI活用レベル(推定)**を付与し、「AImodeログ」タブ（15列：時刻/学年/番号/名前/レベル座標/1目的…8振り返り/AI活用レベル(推定)/raw）に1行追記。
- ⚠️ `raw` の8分解はサーバ側の**自由文パース＝書式依存で脆い**。`log_gas` 側はラベル基準＋全角正規化で堅牢化済みだが、将来は固定区切り or フロント構造化送信がさらに堅牢。

**確定した受け渡し契約（log_gas 設計スレと合意）**
- **同一ブック**：`log_gas.gs` の `SHEET_ID` ＝ `score_gas.gs` の `SPREADSHEET_ID` ＝ `1x3jpH6zynyl-9Pw14XxvoNpza0N17vsLhKgky5dXNJs`（grade book に同居）。
- **タブ**：確定名「**AImodeログ**」。`log_gas.gs` の既定値は `TAB="AIログ"` のため、**本番は `TAB="AImodeログ"` にそろえる**（さもないと空の別タブができ二重化）。`score_gas` 側は**タブ名ではなく列見出し**「AI活用レベル(推定)」で自動検出するので、名前が違っても・万一二重タブでも追従（データ最多のタブを採用）。
- **join キー**：見出し名「学年」「番号」（成績まとめと共通）。相関は**列見出し名で突合**するので列順が変わっても壊れない。
- **AI活用レベルの語彙**：`知識 < 思考 < 議論 < 探求`（＋フリーパス→探求扱い）。`score_gas` は「議論＋探求」を *deep* 回数として集計。
- **責務境界**：`log_gas` は収集＋レベル蒸留まで。振り返り(8番)の「つらさ用語」検知などケアは**grade book 側（本アプリスレ）**で扱う。

```
   基本レーン（成績）                      プロンプトレーン（別コード・同居タブ）
   words/mogi/eiken/me ─▶ score_gas ──┐
   mado-drill/AImode  ─▶ log_gas ─────┤─▶ 同一スプシ（grade book）
                                       │     ├ 成績まとめ/単元/設定 …（score_gas）
                                       │     └ AImodeログ（log_gas）
        └── 学年＋番号 が共通 ＝ 2タブを join して「成績 × プロンプト力」を相関 ──┘
   admin（PIN）：既存ゲート/方針 ＋ プロンプト連携 ON/OFF（app/UI の切り離しスイッチ）
```

### 8.1 接続の5点セット（§8.0 の分離を守ったうえで）

| # | 足す場所 | 既存のお手本 | 内容 |
|---|---|---|---|
| 1 | **学習サーフェス** | `words/`（独立アプリ） | プロンプト練習の出題・評価UI。**独立した `prompt/` フォルダ**にし、ON/OFFで丸ごと隠せる/消せる単位にする。 |
| 2 | **ローカル記録** | `tango_v2` / `shuku_v1` | 例：`prompt_v1 = {best, count, last, skills:{明確さ,文脈,制約,反復改善…}}`（既存キーとは別キー）。 |
| 3 | **集約（同一ブックの専用タブ）** | `handleSummary`／`SUMMARY_COLS` | **専用GAS `log_gas.gs`（別URL）**が成績と**同一ブックの「AImodeログ」タブ**へ追記。`成績まとめ`/`SUMMARY_COLS` は不変。**学年＋番号**で成績と join 可。 |
| 4 | **育成コメント** | `POLICIES` ＋ `guidance()` | 方針 `prompt` を追加し分岐を1本。**ただしフラグ ON のときだけ**マイページに表示。 |
| 5 | **評価ロジック** | `engine.js` の自動採点 / `check_exams.mjs` | 固定正解で測りにくい → **LLM評価（Claude API）をプロンプト専用GAS経由**で（§8.3）。 |

### 8.2 「育成アルゴリズム」を載せる場所
§6 の制御ループ（測る→方針→介入→再測定）が**そのままアルゴリズムの宿主**になる。
- **入力（センサー）**：`records()` の状態ベクトル＋新キー `prompt_v1` のスキル内訳。
- **方針（policy）**：今は先生が手動選択。新アルゴリズムは
  状態から**次に伸ばすべきスキルを自動推定**して動的方針を出せる（適応的カリキュラム）。
- **介入（intervention）**：`praise()`/`guidance()` の1〜2行。
  プロンプト軸では「より良い言い換え例」「不足している制約の指摘」を返すなど、LLM生成の余地。
- **測定（measure）**：次回送信の差分（`mado_snap_v1` 同様のスナップショット）で効果を見る。

### 8.3 プロンプトスキルの自動評価（LLM）— 設計の選択肢
固定正解がない領域なので、評価には外部LLMが要る。CORS回避の既存流儀（`text/plain` POST）に合わせ、選択肢は：
- **(A) プロンプト専用GASを中継（実装済み）**：**`score_gas.gs` ではなく**、別デプロイの `log_gas.gs` が受信・パース・追記する。LLM評価を足すならこの `log_gas.gs` に `action:"prompt_eval"` を置き、Claude API を叩いて採点JSONを返す（鍵はGAS側に隠す）。grade lane のコードは不変。
- **(B) 別サーバーレス**：評価専用の小さなエンドポイントを別途用意（GASのレート/制限を避けたい場合）。これも成績GASとは別系統。
- いずれも**ルーブリック固定**（明確さ/文脈/制約/出力形式/反復改善 などの軸）にして、
  返り値を保存 → **同一ブックの「AImodeログ」タブ**へ集約・マイページのコメントへ流す（フラグON時のみ）。

### 8.4 品質ゲートとの整合
新コンテンツ（プロンプト課題・ルーブリック）も `npm run check` に**検査を1本足す**のが工場流儀。
固定正解がないものは「ルーブリックJSONの構造検査」「サンプル解答がスコア境界を跨ぐか」等で代替検証する。

---

## 9. デプロイ・運用

| 対象 | 方法 | 注意点 |
|---|---|---|
| 静的アプリ | `master` への push で **GitHub Pages 自動反映**（`.nojekyll`） | ビルド不要。`*.xlsx`・ツール・依存は公開から除外。 |
| GAS | コードを貼替 → **[デプロイを管理]→✏️→「新バージョン」** | **保存だけでは反映されない**。URLは不変。`SPREADSHEET_ID` は貼替で消えるので再設定。 |
| 認証 | `TEACHER_PIN`（既定 `"PIN"`／要変更）＝ admin のゲート/方針 | adminは noindex。URLを生徒に教えない。 |
| 版確認 | `GAS_VERSION`（`"unit-gate-2"`）を admin が表示し stale 検出 | 旧版でもゲート対応なら警告しない（`gateOK()`判定）。 |
| 名簿 | `CLASS_SIZE = {"2":40,"3":40}`（未提出者算出の母数） | クラス人数に合わせて変更。 |
| 初期化 | `refreshData()` を手動実行で見出し統一＋古い記録消去 | 学年表記の移行などで使用。 |

GAS URL は3系統（いずれも同一スプレッドシート）：**me/exam/admin = `AKfycbzJ2HThm…`（score_gas）／eiken = 別URL／AImode = `AKfycbySJy2…`（log_gas）**。
> ⚠️ **score_gas と log_gas のURLを取り違えないこと**。admin/me/exam が誤って log_gas を向くと、`kind:"ai_log"` 以外は全部 `{result:"ignored"}` で捨てられ、「？取得できません／保存できませんでした：エラー／生徒が提出できない」が同時発生する（2026-06-29 に実際に発生→URL是正で解消）。両者は**別プロジェクト・別デプロイで常時並走**させる。
**プロンプトモードは別コード系統**（専用GAS `log_gas.gs`／別URL `LOG_GAS_URL`／フロントは `mado-drill/AImode.html`）。ただし**書込先は成績と同一ブックの専用タブ「AImodeログ」**＝意図的統合で、成績と**学年＋番号**で突合できる。`score_gas.gs`／`SUMMARY_GAS_URL`／`成績まとめ` は不変（§8.0）。

---

## 10. 守るべき制約・原則

- **アプリにビルド工程を持ち込まない**（静的HTML/JSのまま。GitHub Pagesで即公開）。
- **CORSプリフライト回避**：GASへのPOSTは必ず `Content-Type: text/plain`、本文はJSON文字列。
- **localStorageはオリジン単位**：3アプリ＋マイページで記録を共有する前提を壊さない。
- **学年・番号は半角限定**（`han()`）：スプレッドシートのソート/突合のため。
- **習熟度は80%以上だけ正式送信**（水増し防止）。記録系は `max` で下げない。
- **teacher-in-the-loop**：単元テストの開閉・学習方針は先生が制御（自動で走らせない）。
- **プロンプトモードは2レーン分離（§8.0）**：基本アプリと疎結合のまま並走させ、
  ① admin の「接続 ON/OFF」でいつでも切り離せる（OFFなら基本アプリは今日と同一・フェイルセーフOFF）、
  ② プロンプト学習データは専用GAS（`log_gas.gs`）で**成績と同一ブックの専用タブ**に集約（**学年＋番号**で突合可・`成績まとめ`/`SUMMARY_COLS` は不変）。
- **コンテンツは必ず `npm run check` を通す**：壊れた出題はCIで止める。
- **生成・改変は小さくPR → 人がレビューしてマージ**（問題工場の鉄則。`master`直push しない）。

---

## 11. ロードマップ（未着手メモ・着手前）

### リスニング練習（**リンク復旧後に着手予定**）
> 既存の模試データ（`mogi/data/*.js`）を流用してよい。新規エンジンは作らず、まず「素材＋運用」から始める構想。
- **(1) 印刷読み上げ**：スクリプトを印刷 → 先生（山田）が読み上げ、生徒は紙で解答。
- **(2) 生徒どうしで出題**：ペア/グループで読み上げ合う（話者役を交代）。
- **(3) AIモード貼り付け→音で聴く**：原稿をAIモードに貼り、リーディング(読み上げ)モード等で再生 → 耳で聞いて解答。
- **(4) 評価まで**：解答（音声/英文）をAIモードに投げ、**発音・正誤を評価**させるところまでをプロンプト化。
- 留意：音声合成はAIモード側（`mado-drill`）で完結させ、基本アプリにビルド工程を持ち込まない原則（§10）を守る。

#### 出題パターンB：長文2回読み→まとめて4問（英検/共通テスト型）＋攻略テクニック
> 長文が読まれ、設問4問程度に**まとめて**解答する形式。長文は通常**2回**読まれる。
> この「2回読み」前提の時間配分のコツを、**解説**したうえで**実際に解かせる**（解説モード＋演習モードの2本立て）。
- **1回目の長文**：メモと全体理解に徹する（一字一句より、流れ・トピックをつかむ）。
- **1回目の設問**：即答できなければ深追いしない。**何が問われているか（設問の意味）だけメモ**して次へ切り替える。
- **2回目の長文＝本番**：1回目でメモした設問を手がかりに、**答えのキーワードをスキャン**することに集中する。
- **2回目の設問**：もう後がない＝この時点で答えは**見つけ終えている**前提。確定して解答する。
- 実装メモ：素材は既存模試/新規どちらでも可。スクリプト（読み上げ原稿）＋設問＋「2回読み」の運用をセットで用意する。

---

_最終更新: 2026-06-29 ／ 本資料は実コード（`me/index.html`・`mogi/assets/engine.js`・`tools/score_gas.gs`・`words/js/app.js`・`admin/index.html`・`factory/`）に基づく。_
