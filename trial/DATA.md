# 送信されるデータ一式（英語学習アプリ）

このファイルは **実コードから自動生成** しています（`node tools/make_data_spec.mjs`）。
手書きの説明とズレないよう、送信側（各HTML）と受信側（`tools/score_gas.gs`）の実体から
項目を抽出し、送信キーと列定義の対応を検算した上で出力しています。

- 生徒用の届き先：生徒用スプレッドシート
- 実証（試用版）の届き先：実証用スプレッドシート（`?site=aso` のときのみ・別ブック）
- どちらも**同じアプリ・同じ項目**を送ります。違うのは届き先だけです。

---

## 0. 送信の全体像

| # | いつ送るか | 送る画面 | 届き先タブ | 送り方 |
|---|---|---|---|---|
| 1 | マイページで「先生に送信」を押したとき（学年・番号が入っていれば自動送信も） | `me/` | 成績まとめ | 1人1行を上書き更新 |
| 2 | 英検アプリで判定テストを終えて送信したとき | `eiken/` | 英検テスト履歴 | 1回ごとに1行追記 |
| 3 | 模試の単元テストを提出したとき（先生が受付を開けている間のみ） | `mogi/exam.html` | 単元テスト記録 | 1回ごとに1行追記 |

これ以外の操作（単語アプリ・読解道場・挑戦モード・入試模試の自己採点など）は
**通信しません**。記録は端末内（localStorage）に留まります。

送信はすべて HTTPS の POST（JSON）で、宛先は Google Apps Script のウェブアプリ1本のみです。
第三者のサーバー・解析ツール・広告タグは使っていません。

---

## 1. 成績まとめ（マイページからの集約・1人1行）

送信キー 48 個（うち `kind`・`ver` は制御用）→ 列 47 個。
先頭の `更新日時` だけはアプリが送らず、受信時にサーバー側で付けます。
「最大値」の列は、これまでの最高値を保持します（記録は下がりません）。

| 列（見出し） | 送信キー | 保持 | 内容 |
|---|---|---|---|
| 更新日時 | `_ts` | 最新 | 受信した日時（**サーバー側で付与**。アプリは送りません） |
| 学年 | `cls` | 最新 | 学年（1〜3）。生徒が入力 |
| 番号 | `num` | 最新 | 出席番号。半角数字のみ |
| 名前 | `name` | 最新 | 名前。**任意**（空でも送信できる） |
| 単語_合計 | `w_all` | 最大値 | 打ち込めた単語の合計 |
| 単語_目標 | `w_goal` | 最新 | 目標語数（2000固定） |
| 単語_基本編 | `w_basic` | 最大値 | 基本編で打ち込めた数 |
| 単語_拡張編 | `w_ext` | 最大値 | 拡張編で打ち込めた数 |
| 単語_1年 | `w_g1` | 最大値 | 1年の語 |
| 単語_2年 | `w_g2` | 最大値 | 2年の語 |
| 単語_3年 | `w_g3` | 最大値 | 3年の語 |
| 模試_211 | `m_c2_1` | 最大値 | 各回のベスト（100点満点） |
| 模試_212 | `m_c2_2` | 最大値 | 各回のベスト（100点満点） |
| 模試_213 | `m_c2_3` | 最大値 | 各回のベスト（100点満点） |
| 模試_321 | `m_c3_1` | 最大値 | 各回のベスト（100点満点） |
| 模試_322 | `m_c3_2` | 最大値 | 各回のベスト（100点満点） |
| 模試_324 | `m_c3_3` | 最大値 | 各回のベスト（100点満点） |
| 模試_323 | `m_c3_4` | 最大値 | 各回のベスト（100点満点） |
| 模試_最高点 | `m_best` | 最大値 | 模試の最高点 |
| 模試_挑戦回数 | `m_done` | 最大値 | 模試に挑戦した回数 |
| 英検_本番Lv | `e_full` | 最大値 | 英検レベルの座標（0〜8）・本番モード |
| 英検_目安 | `e_full_label` | 最新 | レベルの目安ラベル |
| 英検_クイックLv | `e_quick` | 最大値 | 同・クイックモード |
| 英検_平均Lv | `e_avg` | 最新 | 平均レベル |
| 英検_直近級 | `e_recent_grade` | 最新 | 直近に受けた級 |
| 英検_直近正答率 | `e_recent_pct` | 最新 | 直近の正答率 |
| 英検_直近合否 | `e_recent_pass` | 最新 | 直近の合否 |
| 習熟度単語_中2 | `sv_c2` | 最大値 | 正式点（80%以上のときだけ入る） |
| 習熟度単語_中2_回数 | `sv_c2_n` | 最大値 | 挑戦回数 |
| 習熟度単語_中3第2回 | `sv_c3` | 最大値 | 正式点（80%以上のときだけ入る） |
| 習熟度単語_中3第2回_回数 | `sv_c3_n` | 最大値 | 挑戦回数 |
| 習熟度単語_中3第3回 | `sv_c3_3` | 最大値 | 正式点（80%以上のときだけ入る） |
| 習熟度単語_中3第3回_回数 | `sv_c3_3_n` | 最大値 | 挑戦回数 |
| 模試_214 | `m_c2_a1` | 最大値 | 各回のベスト（100点満点） |
| 模試_331 | `m_331` | 最大値 | 各回のベスト（100点満点） |
| 模試_332 | `m_332` | 最大値 | 各回のベスト（100点満点） |
| 模試_岡山1 | `m_ok1` | 最大値 | 各回のベスト（100点満点） |
| 模試_岡山2 | `m_ok2` | 最大値 | 各回のベスト（100点満点） |
| 模試_岡山3 | `m_ok3` | 最大値 | 各回のベスト（100点満点） |
| 模試_岡山4 | `m_ok4` | 最大値 | 各回のベスト（100点満点） |
| 模試_岡山5 | `m_ok5` | 最大値 | 各回のベスト（100点満点） |
| 模試_岡山6 | `m_ok6` | 最大値 | 各回のベスト（100点満点） |
| 模試_岡山7 | `m_ok7` | 最大値 | 各回のベスト（100点満点） |
| 模試_岡山8 | `m_ok8` | 最大値 | 各回のベスト（100点満点） |
| 模試_岡山9 | `m_ok9` | 最大値 | 各回のベスト（100点満点） |
| 模試_岡山10 | `m_ok10` | 最大値 | 各回のベスト（100点満点） |
| 単語_活用 | `w_katsuyo` | 最大値 | 活用編の得点（動詞150×3＋形容詞50×3＝600点満点） |

> 補足：習熟度単語テストは **80%以上のときだけ点数を送り**、それ未満は空欄です（回数は常に送ります）。
> 何度でも挑戦してよい設計にするための仕様で、「低い点を記録に残さない」ことを意図しています。

---

## 2. 英検テスト履歴（英検アプリ・1回ごと）

送信キー：`cls`、`num`、`name`、`ver`、`grade`、`mode`、`score`、`total`、`pct`、`pass`、`ok`、`warn`、`ng`

> このうち `ok`、`warn`、`ng` は**シートに記録されません**（旧版の名残で、受信側が読み捨てています）。

| 列（見出し） | 内容 |
|---|---|
| 日時 | 受信した日時（サーバー側で付与） |
| 学年 | 生徒が入力 |
| 番号 | 生徒が入力 |
| 名前 | 任意 |
| 版 | アプリの版（例：v0.25） |
| 級 | 受けた級 |
| モード | クイック／フル |
| 得点 | 正解数 |
| 満点 | 問題数 |
| 正答率 | ％ |
| 合否 | 合格／不合格 |

---

## 3. 単元テスト記録（模試の単元テスト・1回ごと）

送信キー：`kind`、`exam`、`session`、`cls`、`num`、`name`、`ver`、`score`、`total`、`pct`

> `kind`・`exam`・`session` は受付の照合に使い、`exam`・`session` は列としても残ります。

| 列（見出し） | 内容 |
|---|---|
| 提出日時 | 受信した日時（サーバー側で付与） |
| セッション | 先生が受付を開けた回の識別子 |
| 試験 | 単元テストのID |
| 学年 | 生徒が入力 |
| 番号 | 生徒が入力 |
| 名前 | 任意 |
| 得点 | 素点 |
| 満点 | 配点合計 |
| 正答率(%) | ％ |
| 版 | アプリの版 |

同じ受付回・同じ生徒からの2回目の提出は受け付けません（1回だけ）。

---

## 4. 先生の操作で送るもの（生徒のデータではありません）

先生用コンソール `/admin/` からは、次の操作だけを送ります。いずれも合言葉（PIN）が必要です。

| action | 用途 |
|---|---|
| `status` | 単元テストの受付状態を見る |
| `gate` | 受付を開く／閉じる |
| `policy` / `setpolicy` | 学習方針コメントの取得・設定 |
| `buildcorr` | AI活用レベルと成績の相関タブを作る |

実証用セットの `trial/` には接続確認用の `ping` があります。これは**シートに一切書き込みません**。

---

## 5. 送らないもの（端末内に留まるもの）

| 記録 | 保存先キー | 送信 |
|---|---|---|
| 単語アプリのクリア状況 | `tango_v1` / `tango_v2` | しない（集計した数だけが上の「単語_〜」に入る） |
| 読解道場の修了・ベスト | `dojo_v1` | **しない**（第2弾で検討） |
| 挑戦モードの自己ベスト | `challenge_v1` | **しない**（練習専用のため） |
| 入試模試・模試の答案そのもの | — | しない（点数のみ） |
| 学年・番号・名前の入力値 | `mado_year` / `mado_num` / `mado_name` | 送信時のみ同送 |

そのほか、次のものは**一切扱いません**。

- 氏名の必須化（名前欄は任意。空のまま送信できます）
- メールアドレス・アカウント情報（アプリはログイン不要で、取得する手段を持ちません）
- 位置情報・端末識別子・生体・感情・カメラ・マイクの情報
- Cookie・広告タグ・アクセス解析（第三者サービスへの通信は一切ありません。
  アプリが通信する先は、上記の Google Apps Script のウェブアプリ **1本だけ** です）
- 答案の本文や自由記述（送るのは点数・回数・レベルの数値のみ）

なお、通信そのものは Google のインフラを経由するため、Google 側では一般的な
アクセスログ（接続元IP等）が発生し得ます。**スプレッドシートに記録されるのは
上記の表の列だけ**で、IPアドレスや端末情報は列として残りません。

記録は端末（ブラウザ）ごとに保存されるため、別の端末には持ち越されません。

---

## 6. 実証（試用版）での違い

| | 生徒用 | 実証用（`?site=aso`） |
|---|---|---|
| アプリ | 同一 | 同一 |
| 送る項目 | 上記のとおり | **同一**（`site:"aso"` が1つ増えるだけ） |
| 届き先 | 生徒用スプレッドシート | 実証用スプレッドシート（別ブック） |
| 画面表示 | 通常 | 上部に 🧪 の帯 |

実証モードで送信先が未設定のときは、生徒用に落ちるのではなく**送信そのものを止めます**。
受信側にも安全弁があり、書き込み先が未設定または生徒用ブックと同じIDのときは書き込みません。
